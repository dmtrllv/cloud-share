# Cloud Share

## Setup/Dev
1. Have Postgresql installed.
2. Setup the .env variables.
3. Install all node dependencies `npm i`.
6. Start vite/client bundling `npm run dev`.
4. Watch the server source `npm run watch-server`.
5. Run the server `npm run server` This will auto restart.